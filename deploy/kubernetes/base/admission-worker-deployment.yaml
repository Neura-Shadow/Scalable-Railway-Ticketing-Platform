apiVersion: apps/v1
kind: Deployment
metadata:
  name: railway-admission-worker
  labels:
    app.kubernetes.io/name: railway-admission-worker
    app.kubernetes.io/component: worker
spec:
  # Keep the baseline single-region and single-writer. Enabling or scaling the
  # worker requires an explicit, reviewed production overlay.
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: railway-admission-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: railway-admission-worker
        app.kubernetes.io/component: worker
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: /metrics
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: admission-worker
          image: scalable-railway-ticketing-platform:milestone-3
          imagePullPolicy: IfNotPresent
          command: ["/usr/local/bin/admission-worker"]
          ports:
            # No Service selects this port. Health and metrics remain private
            # to pod-network monitoring and kubelet probes.
            - name: worker-http
              containerPort: 9090
              protocol: TCP
          envFrom:
            - configMapRef:
                name: railway-admission-worker-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: railway-runtime-secrets
                  key: database-url
            - name: REDIS_ADDR
              valueFrom:
                secretKeyRef:
                  name: railway-runtime-secrets
                  key: redis-address
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: railway-runtime-secrets
                  key: redis-password
            - name: ADMISSION_TOKEN_KEYRING
              valueFrom:
                secretKeyRef:
                  name: railway-runtime-secrets
                  key: admission-token-keyring
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /readyz
              port: worker-http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /livez
              port: worker-http
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /livez
              port: worker-http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 12
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
